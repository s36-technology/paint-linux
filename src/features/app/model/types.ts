export interface CurrentFile {
  name: string;
  path?: string;
  date?: Date;
  size?: number;
}

export interface CanvasSize {
  width: number;
  height: number;
}

export interface Point {
  x: number;
  y: number;
}
